<h1><%= "#{t("views.good.edit.title")} " %></h1>

<%= render 'form', good: @good %>

<%= link_to "#{t("views.application.show")} ", @good %> |
<%= link_to "#{t("views.application.back")} " , goods_path %>
