<section class="section">
  <div class="columns is-centered">
    <div class="column is-4">
      <%= form_for resource, :as => resource_name, :url => update_user_confirmation_path, :html => {:method => 'put'}, :id => 'activation-form' do |f| %>
        <%= devise_error_messages! %>
          <h4 class="subtitle is-3"><%= "#{t("views.devise.confirmations.title")}" %><% if resource.try(:user_name) %> for <%= resource.user_name %><% end %></h4>
          <% if @requires_password %>
            <div class="field">
              <div class="control">
                <%= f.label :password,"#{t("views.devise.confirmations.show.choose_pwd")}", class:'label'%>
                  <%= f.password_field :password, class:'input' %>
              </div>
            </div>
            <div class="field">
              <div class="control">
                <%= f.label :password_confirmation,"#{t("views.devise.confirmations.show.confirm_pwd")}", class:'label' %>
                  <%= f.password_field :password_confirmation, class:'input'  %>
              </div>
            </div>
            <% end %>
              <%= hidden_field_tag :confirmation_token,@confirmation_token %>
                <div class="field">
                  <div class="control">
                    <%= f.submit "#{t("views.devise.confirmations.show.activate")}", class:'button is-warning' %>
                  </div>
                </div>
      <% end %>
    </div>
  </div>
</section>
