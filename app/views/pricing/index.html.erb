<section class="section">
  <div class="has-text-centered box">
    <h1 class="title">Pricing</h1>
    <h2 class="subtitle">If you are a pro, subscribe here in order to add your client's reviews </h2>
    <div class="pricing-table">

      <div class="pricing-plan is-link is-active">
        <div class="plan-header">Subscription</div>
        <div class="plan-price"><span class="plan-price-amount"><span class="plan-price-currency">€</span>120</span>/year</div>
        <div class="plan-footer">
           <% if user_signed_in? %>
            <%= link_to 'Subscribe', new_subscription_path(plan: "annual", plan_id: Rails.application.credentials.good_review), class:"button is-fullwidth" %>
          <% else %>
            <%= link_to 'Subscribe', new_user_registration_path, class: 'button is-fullwidth' %>
         <% end %>
        </div>
      </div>

    </div>
  </div>
</section>
